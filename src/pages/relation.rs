use yew::prelude::*;

#[function_component(Relation)]
pub fn relation() -> Html {
    html! {
        <div>
            <h1>{ "Relation" }</h1>
            <p>{ "This is the relation page" }</p>
        </div>
    }
}