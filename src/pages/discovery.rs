use yew::prelude::*;

#[function_component(Discovery)]
pub fn discovery() -> Html {
    html! {
        <div>
            <h1>{ "Discovery" }</h1>
            <p>{ "This is the discovery page" }</p>
        </div>
    }
}